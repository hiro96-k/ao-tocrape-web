# ao-to-crepe-web

ローカルで簡易プレビューする手順:

```bash
# Python3 サーバーでプレビュー
python -m http.server 8000
# ブラウザで http://localhost:8000 を開く
```

追加事項:
- 画像は `images/` に配置してください（例: `images/crepe1.jpg`, `images/truck.jpg`）。
- カレンダーの営業日は `scripts/calendar.js` 内の `openDays` を編集して反映できます。

